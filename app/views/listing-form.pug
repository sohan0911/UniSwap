doctype html
html
  head
    title New listing – UniSwap
    link(rel="stylesheet", href="/styles.css")
  body.page-shell
    p
      a(href="/listings") ← Back to listings
    h1 Add a listing
    if error
<<<<<<< Updated upstream
      p.error-msg= error
    form(method="POST", action="/listings")
      p
        label(for="user_id") Seller user ID
        br
        input#user_id(type="number", name="user_id", min="1", value=(body && body.user_id) || "1", required)
      p
=======
      .alert.alert-error= error

    form(method="POST", action="/listings" novalidate)
      p.text-muted Published as #{loggedInUser ? loggedInUser.name : 'Unknown user'}
      .form-group
>>>>>>> Stashed changes
        label(for="title") Title
        br
        input#title(type="text", name="title", value=(body && body.title) || "", required)
      p
        label(for="description") Description
        br
        textarea#description(name="description", rows="5", cols="50", required)
          = body && body.description ? body.description : ""
      p
        label(for="tags") Tags (comma-separated)
        br
        input#tags(type="text", name="tags", placeholder="books, math, used", value=(body && body.tags) || "")
      p
        button.btn.btn-primary(type="submit") Publish listing
